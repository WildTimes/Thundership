using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Teste : MonoBehaviour {
   
   /* public DialogosData[] datas = {};
    public List<DialogosData> dtlist;

    void Start()
    {
        DataPool.initpool();

        //datas = DataPool.m_sheet.dataArray;

       //dtlist =new List<Planilha1Data>(datas);

        dtlist = new List<DialogosData>(datas = DataPool.m_sheet.dataArray);
            
    }

    void Update()
    {
        if (Input.GetKeyDown(KeyCode.A))
        {
            print("");
        }
    }
}

public class DataPool
{
    public static Dialogos m_sheet;


    public static void initpool()
    {
        m_sheet = Resources.Load<Dialogos>("Data/Excel/Dialogos"); // precisa estar dentro da pasta resources no Assets
        Debug.Log("Ta indo");
    }*/
}
